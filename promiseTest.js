'use strict'
const promise = require('promise');
const fs = require('fs');

function test() {
    return new promise(function (fulfill, reject) {
        var db =
    })
}

async function asyncTest() {

}
